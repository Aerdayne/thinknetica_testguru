<tr>
  <td><%= counter %></td>
  <td><%= render_icon(badge.image_path) %></td>
  <td><%= link_to badge.name, admin_badge_path(badge) %></td>
  <td><%= badge.description %></td>
  <td><%= badge.image_path %></td>
  <td><%= badge.criterion %></td>
  <td><%= badge.value %></td>
  <td>
    <%= link_to 'Edit', edit_admin_badge_path(badge), method: :get, class: 'btn btn-primary form-inline-link' %>
  </td>
</tr>